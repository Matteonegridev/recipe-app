import Register from "../auth/Register";
import Login from "../auth/Login";

function Auth() {
  return (
    <div className="mt-30">
      <Register />
      <Login />
    </div>
  );
}

export default Auth;
